# Personal, per-machine Claude Code settings (project-shared settings.json IS tracked)
.claude/settings.local.json

# Go build/test artifacts
/bin/
*.test
*.out

# goduct CLI binary (root) and generator outputs in examples.
# The testdata/expected/ goldens are the source of truth; a
# committed examples/<fw>/api/goduct_routes.go would silently
# diverge from them.
/goduct
examples/*/api/goduct_routes.go
